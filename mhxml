#
#  xinetd file for running the image server
#
service mhxml
{
        socket_type     = stream
        wait            = no
        user            = root
        server          = /usr/local/bin/mhWrap
        disable         = no
}
